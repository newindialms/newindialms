<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_program_manager_menu"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:background="@color/textColor"
        app:itemBackground="@drawable/menu_item_color"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_program_manager_menu"
        app:itemIconTint="@color/textHintColor"
        app:itemTextColor="@color/textHintColor"
        app:menu="@menu/activity_program_manager_menu_drawer" />

</android.support.v4.widget.DrawerLayout>
