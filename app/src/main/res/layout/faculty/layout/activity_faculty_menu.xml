<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/faculty_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/textColor"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_faculty_menu"
        android:id="@+id/app_bar_faculty_menu"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/faculty_nav_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:background="@color/textColor"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_faculty_menu"
        app:itemBackground="@drawable/facultymenu_item_color"
        app:itemIconTint="@color/textHintColor"
        app:itemTextColor="@color/textHintColor"
        app:menu="@menu/activity_faculty_menu_drawer" />

</android.support.v4.widget.DrawerLayout>
