<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/student_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/textColor"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_student_menu"
        android:id="@+id/app_bar_student_menu"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/student_nav_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:background="@color/studentwindowBackground"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_student_menu"
        app:itemBackground="@drawable/studentmenu_item_color"
        app:itemIconTint="@color/studentcolorPrimaryDark"
        app:itemTextColor="@color/studentcolorPrimaryDark"
        app:menu="@menu/activity_student_manager_menu_drawer" />

</androidx.drawerlayout.widget.DrawerLayout>
